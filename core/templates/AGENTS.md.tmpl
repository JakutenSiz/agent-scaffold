# {{REPO_NAME}} — agent notes

Short, repo-specific facts only (about 50 lines; real traps beat the budget). Rules live elsewhere and are linked, not copied:
`{{ROOT_RELATIVE}}CLAUDE.md` (project guide) · `{{ROOT_RELATIVE}}docs/agent-working-rules.md` ·
`{{ROOT_RELATIVE}}docs/memory-bank-guide.md`.

## What this repo is
<!-- fill: 2–3 lines. What it owns, what it must not do (e.g. "does not call provider APIs; the backend does"). -->

## Read first
- `{{MEMORY_BANK_DIR}}/repoMap.md` — where things are
- `{{MEMORY_BANK_DIR}}/activeContext.md` — what is in flight
<!-- fill: one or two more entry docs if they exist (architecture doc, runbook). -->

## Commands
```
<!-- fill: install / dev / lint / type-check / test / single test — exact, copy-pasteable -->
```

## Gates before you say "done"
<!-- fill: the ordered list for this repo, or "see /gate-ritual". -->

## Traps
<!-- fill: ≤ 5 dated one-liners: things that silently go wrong (Node version, env var that shrinks a suite, flag that must never be used on the whole tree). -->

## Language
Prose and commits in {{PROSE_LANGUAGE}}; match the file you edit.
