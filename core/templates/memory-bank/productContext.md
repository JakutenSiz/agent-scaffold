# Product Context

*Belongs here: WHY this repo exists — the problems it solves, the flows it supports, the domain
vocabulary in play, and business rules that shape implementation. Does NOT belong here: HOW it is
built (see `systemPatterns.md`) or WHAT is stable/scoped (see `projectbrief.md`).*

---

## Problems This Repo Solves

<!-- fill: the user-facing or system-facing problems this repo exists to solve. Derive from
     product docs, top-level feature folders, and route/endpoint names. -->

## Major Flows / Capabilities

<!-- fill: the handful of major flows a user or calling system exercises (e.g. "sign up",
     "create campaign", "run a nightly sync"). One line each, with the entry point if known. -->

## Expected Behavior & Goals

<!-- fill: the UX or system-level goals implied by the code (performance targets, tone,
     reliability expectations) — only include what is actually evidenced, not assumed. -->

## Domain Language / Glossary

<!-- fill: domain-specific terms/acronyms used throughout the codebase and what they mean here
     specifically (a generic word can have a narrow local meaning). -->

## Business Rules That Affect Implementation

<!-- fill: rules that are not obvious from types/schemas alone but constrain how code must behave
     (e.g. an invariant enforced only in application code, a rule tied to billing/compliance). -->
