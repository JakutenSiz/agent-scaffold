# Project Brief

*Belongs here: the stable identity and scope of this repo — what it owns, what it explicitly does
not own, and the external systems it touches. Does NOT belong here: current work status (see
`activeContext.md`/`progress.md`), architecture detail (see `systemPatterns.md`), or command
references (see `techContext.md`). This file should stay stable — it changes rarely.*

---

## Responsibility

<!-- fill: 2-5 sentences on what this repo is responsible for. Derive from the top-level README,
     package/manifest name+description, and the shape of the top-level directories. -->

## Out of Scope (Not Responsible For)

<!-- fill: what this repo explicitly does NOT do, especially work that a sibling repo/service owns
     instead. Look for outbound API calls / client SDKs to other internal services — whatever they
     call is probably owned elsewhere. -->

## Domain / Business Purpose

<!-- fill: one paragraph on the business or product problem this repo exists to serve. -->

## Top-Level Goals

<!-- fill: 3-6 bullets on what "success" looks like for this repo. -->

## Repository Boundaries

<!-- fill: what lives inside this repo vs. in a separate repo/service/package it depends on or is
     depended on by. If this is part of a multi-repo project, name the sibling repos and one line
     on how each relates. -->

## Critical External Systems

<!-- fill: databases, third-party APIs/providers, queues, auth providers, deploy targets this repo
     depends on or serves. One line each: what it is, why this repo needs it. -->
