# System Patterns

*Belongs here: HOW the repo is shaped — architecture, module boundaries, flows, intentional design
patterns, and anti-patterns to avoid. Explain architecture, not every file (that is `repoMap.md`'s
job). Does NOT belong here: reusable helper inventories (see `capabilityRegistry.md`) or command
references (see `techContext.md`).*

---

## High-Level Architecture

<!-- fill: one diagram-in-words or short paragraph on the overall shape (layered, domain-first,
     event-driven, etc.) and why. -->

## Modules / Layers

<!-- fill: the major modules/layers and what each owns. Name real directories/packages. -->

## Data & Control Flow

<!-- fill: the important paths a request/job/event takes through the system, start to finish. -->

## Design Patterns In Use

<!-- fill: patterns intentionally used here (e.g. repository pattern, adapter per provider,
     advisory locks instead of a queue) and where to see a canonical example of each. -->

## Domain / Layer Boundaries

<!-- fill: boundaries that must not be crossed casually (e.g. "layer X never imports from layer Y
     directly"). -->

## Anti-Patterns To Avoid

<!-- fill: patterns explicitly rejected here, and what to do instead. -->

## Extension vs Modification Rules

<!-- fill: when to extend an existing abstraction vs. when a new one is actually warranted. -->
